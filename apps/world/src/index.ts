// apps/world — the world server (embodiment layer): scheduler + API + SSE.
// Stub entry point; the world-engine, agent-runtime, tools, and scheduler
// phases fill this in. Keeps the workspace resolving and typecheck/build green.
export {};
