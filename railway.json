{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @town/contract build && pnpm --filter world build"
  },
  "deploy": {
    "startCommand": "node apps/world/dist/db/migrate.js && node apps/world/dist/db/seed.js && node apps/world/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ALWAYS"
  }
}
