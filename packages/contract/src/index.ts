// @town/contract — shared zod schemas + types (event taxonomy, REST shapes,
// agent/location IDs) imported by both apps/web and apps/world.
// Filled in by the contract phase; stub keeps the workspace resolving.
export {};
