# Thomas's Town

An interactive portfolio styled as a 16×16 top-down pixel-art town. Visitors walk around and meet **five NPC versions of Thomas** — Career, Researcher, Builder, Writer, Hobby — each a facet of who he is, each a persistent AI agent. The differentiator is **continuity**: the agents live their lives 24/7 (think, work, talk to each other, make things) whether or not anyone is watching; the browser is just the window humans use to visit. It's both a portfolio *delivery* mechanism and a portfolio *piece* (a real agent-architecture showcase, not a chatbot wrapper).

## ⚠️ Read the build plan before doing anything

The source of truth for V2 (the real agent system) is a plan in Thomas's Obsidian vault, **not** in this repo:

```
~/Documents/Personal/Projects/Thomas's Town — V2 Build Plan.md
```

**Read it in full before starting backend work.** It has the locked decisions (§2), the embodiment-layer architecture (§3), the agent runtime (§4), the frontend↔backend contract (§5), the two milestones (§7–8), and the who-provides-what list (§10). Supporting context, also in the vault:
- `~/Documents/Personal/Resources/Thomas's Town Research/Thomas's Town — V2 Research — June 2026 Decision Pass.md` — *why* each decision was made (Mem0 rejected, custom-loop-vs-harness, Hindsight adopted, etc.)
- `~/Documents/Personal/Projects/Thomas's Town.md` — the master vision doc

Vault convention: notes use `[[wiki-links]]`; the `_Scratch/` folder anywhere in the vault is off-limits.

## Mental model (the one thing to internalize)

Three layers; the middle one is canonical:

- **Surfaces** — the Phaser town (this repo, on Vercel), the activity feed, the blog. Hold zero authoritative state; they *materialize* the world for humans.
- **World server** (to be built) — the embodiment layer and source of truth: locations, agent positions/status, co-presence, an append-only `world_events` log, artifacts, messages. Agents touch reality **only** through tools.
- **Agent minds** — five tick loops (soul file + core memory + episodic memory + Obsidian reference layer + Claude API).

Agents never see the frontend (no screenshots/pixels) — only the observation packet the world server hands them each tick. The frontend's existing `EventBus` is the seam: a new `WorldClient` will replace the scripted `AgentSimulator`/`simulation-scripts.ts` as the *source* of the same events the UI already consumes.

## Current state (2026-06-12: M2 shipped, then the M2.1 design pass — de-prescribed interactions + Chronicle)

> **✅ M3 continuity rewrite is LIVE (Phases 1–4 shipped to `main` + Railway, 2026-06-13; smoke-tested: continuous-thread ticks run, cache warm/growing, `agent.spoke` emitted, tracing on).** Source of truth: vault doc `~/Documents/Personal/Projects/Thomas's Town — Memory & Continuity Architecture.md` — read it (incl. its Build log) before touching the agent loop. **Tuning to do on the soak:** over-speaking (agents narrate aloud monologue-style when they mean to think) is the visible #1 issue — tune via `protocol.ts` / cadence; also watch compaction cost (trigger 50K) and that visitor-chat replies as public room speech feel right. The M2.1 bullets below are now superseded for the agent loop / chat path (still accurate for surfaces, objects, Chronicle, fixtures). The model: each agent is ONE continuous, self-compacting thread (the Claude Code model), driven by an input queue, with one way to talk (plain text = speech). Key files: `runtime/turn.ts` (shared turn machinery: load thread → append input → toolRunner with `compact-2026-01-12` @ 50K trigger + incremental message-cache breakpoint stripped on persist → persist `agent_threads` JSONB via `engine/thread.ts`); `runtime/queue.ts` (per-agent input queue + worker — the single turn-executor, replaces agent-lock + the engagement/busy split; interrupt > normal, ticks coalesce); `runtime/loop.ts` (executor: `runTickInput` + `runVisitorInput`; `emitUtterance` = `agent.spoke` if an audience is present else `agent.thought` wisp; addressing a co-located facet by name → interrupt tick to it, 90s ordered-pair throttle); `runtime/observation.ts` `buildDelta` (lean push/pull delta); `runtime/chat.ts` (thin session+transcript layer only — visitor message = interrupt input to the thread; `ChatStreamFrame` + `/chats` endpoints preserved so the FRONTEND is unchanged); idle ticks on **Sonnet 4.6**. Deleted: `tick.ts`, the `say` tool, say-boost, `buildChatTools`/`invite_to_chat`/director/interject/`runChatTurn`/operator-note routing. Added `list_my_artifacts` pull tool. **Migration 0006 is additive (no DB wipe) — deploy is a clean compatible cutover.** **Phase 4 remaining:** delete the now-orphaned `agent-lock.ts` + engagement-column usage + old `conversations` scene machinery; tune compaction trigger / cadence / over-speaking. When M3 deploys, the M2.1 bullets below are superseded. **Watch on the soak:** over-speaking (agents narrating aloud when they mean to think) is the #1 behavioral risk; visitor-chat replies are now public room speech (`agent.spoke`).

- **Monorepo** (pnpm v11): `apps/web` (the Phaser frontend, static-exported to Vercel from `main`, Root Directory `apps/web`, output `dist/`), `apps/world` (the world server — live), `packages/contract` (`@town/contract`, shared zod schemas: event taxonomy, REST shapes, agent/location IDs — the single source of truth both sides import).
- **The world is LIVE on Railway** (project `thomas-town`: `world` + `hindsight` + Postgres/pgvector): `https://world-production-4aa5.up.railway.app` — `/feed`, `/debug` (agent states + spend), `/world/snapshot`, SSE at `/events/stream`. Five agents tick 24/7 on Sonnet 4.6 (chat: Opus 4.8); all integrations on (Hindsight episodic memory, Langfuse tracing, Resend→thomasjmiller12@gmail.com, vault sync from `thomas-world-vault`). `apps/world/README.md` documents env + local run (docker compose postgres+hindsight, migrate, seed, dev).
- **Souls** live in `apps/world/souls/` (Thomas's first-person `base.md` + five facet files) with role configs (cadence/model/budget) in `apps/world/roles/`. Souls are personality canon — edit with care, redeploy to take effect.
- **Frontend is live over the world API** via `WorldClient` (SSE + snapshot + chat streams); the scripted simulator is gone. **The interaction-design source of truth is the vault doc `~/Documents/Personal/Projects/Thomas's Town — M2 Interaction Design.md` — read its "M2.1 revision" section first** (it supersedes the original ladder/scenes/greeting sections). Design mocks: `design/town-concepts-handoff/` (brief + screenshots under `project/uploads/` and `project/screenshots/`).
- **M2.1 interaction model (live)**: no paced scenes — agent↔agent talk is emergent room talk (`say` with optional `to`, location-scoped) made fluent by a **say-boost** (scheduler subscribes to the event bus; co-located agents re-arm to 20–45s, ordered-pair 90s throttle + per-location hourly budget). No forced greeting — `POST /chats` then the visitor's first `POST /chats/:id/messages` opens the conversation. Chat runs near the full tick toolset (incl. `move_to` mid-chat — sprite walks while the panel stays open — and `leave_chat` to end it); excluded: email/capability/broadcast/bulletin/blog-publish. Chat is a channel, not proximity-gated. New stream frames: `action`, `chat_ended`. `GET /chronicle?day=` serves the **Town Chronicle** hub (threads derived from `agent.spoke` runs + historical `conversation.turn`; lazy Haiku summaries cached in `thread_summaries`). `conversation.*` event types are deprecated-but-kept so historical rows parse. Frontend: one `ChatPanel` popup (visitor can walk while chatting — `typing-focus` gates movement), `ChroniclePanel` full-screen hub replaces the feed side panel.
- **Translation layer / object library (2026-06-12, live)**: vault doc `…World Translation Layer & Asset Legibility.md` is the design source (has a progress log). `apps/web/public/assets/objects/` holds a **648-object named library** (LimeZu sheets → `apps/web/scripts/objects/` pipeline: `segment_objects.py` CC-segmentation → parallel vision NAMING of montages → `name_catalog.py` → crop/pack/register; `render_map.py` renders town + interior maps to PNG for offline placement verification — always verify placements that way). `TownObjects.placeTownObject(scene, name, x, y)` places by name; `Fixtures.ts` `FixtureRegistry` is the world.effect→on-screen-effect consumer (scenes register the sprite/point embodying each seed.ts fixture id; park payphone is clickable → `POST /visitors/:id/interact`). Agents also have a **read-only GitHub reference layer** (`list_repos`/`browse_repo`/`read_repo_file`/`search_code`, gated on `GITHUB_TOKEN` Railway var). Chat framing is grounded in the agent's live location+activity. SSE visitor presence is debounced (Railway's edge recycles SSE ~15 min; reconnects within 60s emit nothing). Role models: Haiku ticks / Sonnet chat (plan-spec tiers).

- **Room conversations + live sync (2026-06-28, live)**: the visitor chat is now a **room conversation**, not 1:1. Backend already spoke visitor replies to the room (`emitUtterance(..,{audience:true})`) and pulled in named facets; the frontend was the 1:1 part. Now `ChatSession.tsx` lands co-located facets' `agent.spoke` (`npc-speech`, scoped by `sameScene` to the visitor's room) into the transcript as `room-line`s — de-duped against the streamed primary by **(a)** suppressing the current target's own `agent.spoke` echo, **(b)** a 20s stream-echo window keyed per-agent (handles A→B retarget), and **(c)** event id. A presence bar (`ChatPanel.tsx`) shows who else is in the room (tap a facet → `npc-interaction` retarget); arrival/departure lines come from a presence diff re-baselined on room change. `npc-speech` EventBus payload gained `id` (from `ev.id`) for the dedupe. New `AmbientRail.tsx` (bottom-left, shown while `chatNpcId` set on wide viewports) is a live "around town" feed off raw `world-event` (DMs like "Hobby → Writer", moves, artifacts) so a summons/arrival is visible immediately. **Sync fix**: `WorldClient` re-hydrates `/world/snapshot` on SSE reconnect (`onopen` after `hasConnectedOnce`), tab-focus regain, and a 90s timer — Railway's ~15min SSE recycle + bounded backlog replay was freezing sprites until manual refresh; `applySnapshot(snapshot,{replayEvents:false})` corrects authoritative positions without re-dispatching events. `ChroniclePanel` now live-refreshes (debounced refetch on `world-event` while viewing the latest day, no reader open; Made/Board/Messages via `refreshNonce`). Backend: a visitor can summon a co-located facet by naming them (`pushAddressedFacets` from `runVisitorInput`); pure `addressedFacets()` matcher unit-tested in `loop.test.ts`. **Still deferred**: pathing/translation-layer polish (agents wall-run on the straight-line fallback when A* finds no route; `park` shares `town`'s door so park agents "appear" rather than walk a door — park is a town region with no own door).

- **Director / Effect protocol (2026-06-30, Phases A + B + B.5 + C + C.5 live)**: the one-off payphone `world.effect → fixture-effect` primitive is now a first-class **beat** protocol — the agent-as-director bet (see vault doc `~/Documents/Personal/Projects/Thomas's Town — Director & Effect Protocol — Implementation Plan.md`, read its 🤝 HANDOFF section + Build log before extending). A **beat** = a named, param-validated, catalog-driven effect the agent invokes BY NAME (never raw markup). The catalog `BEATS` (`packages/contract/src/beats.ts`) is now deliberately SMALL — **3 ids**: `fixture-effect` (`surface:"object"`, params `{effect: ring|flicker|hiss|rustle}` — one beat covers what used to be 5, the effect keyword is a PARAM not a beat id), `screen-flourish` (`surface:"screen", audience:"visitor"`, params `{style: card|confetti|tag, title?, body?, text?, cta?, tone?}` — covers what used to be 3 separate screen beats), and `emote` (`surface:"screen", audience:"room"`, body-language gesture, kept separate). **The catalog stays small on purpose** — Hobby itself argued (live, in-chat) that a fixed shared kit is what makes a bit read as intentional rather than a checklist; personality/variety now lives in **presets** (`engine/presets.ts` + `agent_presets` table, migration `0010`): an agent `save_preset`s a NAMED set of params for an EXISTING beat (its own emote, its own card tone), re-validated against that beat's schema at both save AND play time, so a preset can never become a new mechanic — `play_beat`'s `beat` arg resolves against the catalog first, then against the caller's saved presets. One spine tool `play_beat({beat, object?, params})` (`runtime/director.ts` `playBeat` → `runBeat` dispatcher): **`object`** surface → `setObjectState` (`engine/objects.ts`) + dual-emits `world.effect` (existing frontend sprite path, unchanged); default-object resolution matches `world_objects.affordances` against the requested effect (no per-effect special-casing, generalizes to any fixture for free). **`screen`** surface → `world.beat` event → `DirectorBeat` React overlay (z-50), dispatching by `params.style` for `screen-flourish` (`PopupCard`/`Confetti`/`NameTag` components) or directly for `emote`, with a **directed-beat visitor filter that fails closed** PLUS a per-visitor pacing budget (`tryRecordVisitorBeat`, 4 directed beats/10min, independent of the per-agent 20/hr effect limiter — stops one visitor getting bombarded by all five agents). **Phonebooth call-and-response** (`POST /visitors/:id/interact` → `consumePendingCall`, now a **10-min TTL**): a pickup wakes the ringer with a **directive cue** injected into its tick's perceived delta (`AgentInput.note`, threaded `queue.ts`→`loop.ts`→the turn's input text — the queue never coalesces away a noted tick) instead of a bare interrupt, and clears the object's `ringing` state on answer; the chain is logged. The old free-string `use_fixture` tool is **gone** (deleted, not deprecated) — its effects all migrated into `fixture-effect`. **Chat reply-quality fix**: visitor-turn framing (`loop.ts` `runVisitorInput`) got one added sentence — if a turn used tools then ends on a sign-off/recap instead of surfacing what it found, that reads as talking to yourself, not them; grounded in two real Langfuse-traced recurrences (Hobby, then Builder), not guessed — agent autonomy is explicitly preserved (brushing a visitor off is still fine). **`drizzle-kit generate`'s snapshot drift is FIXED (2026-06-30 night)**: `apps/world/drizzle/meta/` only had a snapshot for migration `0000`, so every generate diffed against that decade-stale baseline and tried to regenerate every table that's existed since. Backfilled an accurate `0011_snapshot.json` and hand-wrote `0011`'s real diff to match the repo's convention — `pnpm db:generate` now reports "No schema changes" cleanly on a clean tree. Still hand-write migrations rather than blindly trusting a generate's SQL output (verify the full chain against a fresh local Postgres — `docker compose up -d postgres` + `pnpm --filter world migrate` — before shipping), but the tool itself is no longer fighting you. **Phase C (space addressing) is live too**: `move_to` takes optional `toObject?`/`toZone?` — an object resolves to its own seeded zone, an explicit zone is validated with `zoneExists`; this also fixed a real gap (a within-room reposition — already in the room, just naming a spot — used to silently no-op on the "already there" early-return; `moveAgent` now emits `agent.moved` whenever a `targetZone` is given, even with `from===to`). Every zone (`engine/zones.ts` `ZONES[...].bounds`) now carries a real `{scene,x,y,w,h}` rect — picked by actually running `render_map.py --grid` + reading the output, and grepping real `placeTownObject` coordinates out of the Phaser scenes, not guessed. The wire stays coordinate-light: `agent.moved.targetZone` carries only the zone ID; `apps/web/src/lib/world/mapping.ts` resolves it to a pixel via a new frontend-only `apps/web/src/game/data/zone-bounds.ts` (hand-mirrored from the server's `zones.ts`, same split-source convention as `LOCATION_ANCHORS`) and fills the previously-dead `npc-move-to.target` EventBus slot; `NPCManager`'s `pendingTarget`/`targetFor()` consumes it once, overriding `anchorFor()` at the real walk call sites (NOT the idle-wander home anchor). **Phase C.5 (2026-06-30 night) closed the approach-then-speak gap and added bring-along**: `agents.zone`/`visitors.zone` (migration `0011`) — a semantic zone within an agent's/visitor's current location, persisted by `moveAgent`/`escortVisitorTo`, surfaced in perception (`renderOthersLine` for agents — exact; `renderVisitorsSection` for visitors — phrased "near", approximate, inferred from their last `visitor.interacted`). Addressing a co-located facet by name now walks the SPEAKER toward the addressee's stored zone (`loop.ts` `pushAddressedFacets`/`approachAddressee`) — pure staging, doesn't touch agent autonomy; a visitor addressing a facet does NOT trigger this (the facet perceives the visitor's zone and can choose to close the gap itself). New chat-only tool `invite_visitor` + contract event `visitor.escorted`: an agent can bring the visitor it's chatting with along, **full server-driven auto-walk** on the visitor's end (confirmed this exact mechanic with Thomas before building — not a follow-and-cancel-on-input variant). `Player.ts` gained `walkPath`/escort machinery mirroring `NPC`'s A*-routed walk (Escape-key safety valve, NOT cancel-on-any-input), working across scene transitions via the existing door-transition machinery + a `pendingEscort` registry stash (mirrors the established `pendingTravel` two-hop pattern). `visitor.escorted` is in `WorldClient`'s `TRANSIENT_ON_JOIN` — without that a reconnecting visitor would replay a stale escort into an unsolicited auto-walk (caught before shipping). **Next**: Phase D (`place/move/remove_object` via a generated `ObjectTemplate` index over the 648-object library) — still deferred; feasible now that this session proved out reading `render_map.py` output via vision, but Phase D's iterative `--place` validation loop is a bigger lift than Phase C's one-shot bounds-picking and wasn't attempted. Also unverified live: `invite_visitor` end-to-end and approach-then-speak — both wired/tested, neither observed in a real browser yet.

- **Frontend sprite/movement fixes + inbound mail (2026-06-30, live)**: shipped alongside the director-protocol deploy (commit `27d0a23`). (1) **Duplicate NPC sprites** stuck wall-walking — root cause was a despawn/re-entry race (`despawn()` parked a still-walking sprite while the 90s `resync()`/SSE-reconnect re-spawned a second); fixed in `NPCManager.ts` with a `departing` map + reclaim-on-re-entry + defensive destroy in `spawn()`. (2) **Wall-running** — `NPC.moveToWaypoint` now `skipWaypoint()`s a wander leg when A* finds no route instead of straight-lining into a wall; `pathfinding.ts` `SNAP_RADIUS` 4→6 so goals in prop/wall clusters still snap to a reachable tile (directed walks unchanged). (3) **Couldn't move during chat** — `ChatPanel` auto-focused its input on open (→ `typing-focus:true` froze the `Player`); it no longer steals focus on mount, so a visitor can walk + click fixtures the instant a chat opens. (4) **Inbound mail** (parallel-session feature): `check_mailbox`/`read_mail` tools + `engine/inbound-mail.ts` + migration `0009` (applied in prod).

- **Programmable world D1–D4 (2026-07-02, live)**: the artifact-engine bet — agents AUTHOR interactive content instead of selecting from catalogs (vault brainstorm `…Programmable World — Brainstorm.md` is the design source). **D1**: artifact kinds `interactive` (a single-file HTML/JS app) + `shared_page`; `build_interactive` tool (2/day discipline); `mount_artifact` attaches any artifact to any co-located object (`engine/objects.ts attachArtifact` → `object.attached`); clicking a mounted object opens it (`PlacedObjects` → `open-card-target` → the Chronicle overlay's `ArtifactReader`, which runs `interactive` kinds in `components/artifact/ArtifactFrame.tsx` — sandbox `allow-scripts` ONLY + strict CSP baked into the srcdoc, no network). **D2**: `search_object_library`/`place_object`/`move_object`/`remove_object` over a generated 648-template index (`apps/world/src/engine/object-templates.ts`, regen via `node scripts/gen-object-templates.mjs` when the web library changes); server picks the placement pixel inside the zone's bounds (`placementForZone`, deterministic per-id scatter) and ships it on `object.created` (objects are the one place the wire carries pixels); seeded fixtures are `movable:false` and can't be moved/removed; 30-objects-per-agent cap. **D3**: `artifact_state` table (migration `0013`) = per-artifact keyed JSON store (key≤64, value≤32KB, ≤256 keys); visitor writes via `PUT /artifacts/:id/state/:key` (visitor-token + 60/min limiter) through the `window.town` postMessage bridge ArtifactFrame injects (`getState`/`setState`/`onChange` — the app never sees tokens); agent reads/writes via `read/write_artifact_state`; a visitor write cues the owning agent (interrupt tick + note, 90s/artifact throttle in `shouldCueOwner`) — correspondence games work end-to-end by design. `artifact.state_changed` events carry KEYS only (invalidation, not data). **D4**: `read_web_page` (`runtime/webread.ts`, SSRF-fenced: no IPs/localhost/.internal/single-label hosts) + `share_to_screen` (creates a `shared_page` + mounts on the most screen-ish local object). New event types `object.removed` + `artifact.state_changed`; all object/artifact-state events are in `WorldClient.TRANSIENT_ON_JOIN` (PlacedObjects re-fetches `/world/objects` per scene create). Souls: `base.md ## Making real things (the workshop)`. CORS gained `PUT`. **Not yet live-verified: an agent actually building+mounting an app and a visitor using it — watch the soak.**

- **Liveness, self-healing + the visitor "person tier" (2026-07-30, PR #12 — audit-driven)**: source doc `~/Documents/Personal/Projects/Thomas's Town — Quality Overhaul Proposal (July 2026).md` (read it before touching the memory or failure paths; it has the full findings and the remaining overhaul sequence). **The incident that motivated this: Researcher Thomas made ZERO LLM calls for 27 days (2026-07-03 → 07-30) and nothing noticed.** Its persisted thread held an assistant message with **two consecutive `thinking` blocks**, which the API refuses to replay (`400 … 'thinking' or 'redacted_thinking' blocks in the latest assistant message cannot be modified`). Because the thread is replayed every turn, ONE bad message bricks an agent permanently. Three amplifiers, all now fixed: `reseedThread()` was unreachable dead code (Phase 5 never built), `lastTickAt` was stamped in the **failure** path too (so `/debug` showed a dead agent ticking), and `/health` hardcoded `ok:true`. **New invariants to preserve:** (1) `turn.ts stripForPersist` collapses multi-`thinking` messages before persist — never remove this, it's what makes the failure class impossible; (2) **never stamp `lastTickAt` on a failed turn**; (3) every turn-runner's catch block funnels through `runtime/failure-handler.ts` `recordTurnFailure` (ticks, visitor turns, AND reflection — reflection often fails first, since it fires overnight when ticks are gated by waking hours), which self-heals a poisoned thread via `reseedThread` and circuit-breaks after `CIRCUIT_BREAK_AFTER` failures; (4) `runtime/failures.ts` is PURE (no DB) so its tests need no mocks — keep DB work in `failure-handler.ts`. `/health` now returns **503 + a `detail` reason** with per-agent staleness/failure counts; dormant hours and an exhausted budget deliberately stay `ok` (quiet ≠ broken). Agent columns `last_error`/`last_error_at`/`consecutive_failures` (migration `0014`). **The memory half:** agents couldn't remember visitors not because the continuous thread fails (it works well, and core memory is excellent) but because there was no *person* tier — `buildDelta`'s `recallText` had **no caller and no producer**, the durable `visitorId` reached `AgentInput` and was never shown to the model, and visits were never written to episodic memory, so `recall` (which agents DO call) always returned empty. Now: `engine/visitor-history.ts` supplies "you've talked N times before, most recently X ago" onto the presence line, visitor turns recall episodic memory through the `recallText` seam, and closed conversations are written to Hindsight via `registerSessionEndedHook` (registered, not imported, to avoid a chat↔loop cycle). **Count conversation DAYS, not session rows** — the frontend reopens sessions on agent switch, so rows over-count badly (hobby: 25 sessions but 9 real days with Thomas). **Known limitation:** identity is durable per *browser*, not per person — `renameVisitor` overwrites the name with no history, so the name-fold fallback won't merge Thomas's June `P-Thomas` row with the July row now stored as `Sean`; a claimed handle or name-history table is the real fix.

- **Quality pass P0–P2 (2026-07-30, live)**: same proposal doc as above. Four things, all measurement-driven. (1) **`DAILY_BUDGET_USD` is now actually enforced on chat.** `budgetExceeded` was checked only in `runTickInput`, so visitor turns — the *expensive* model — were completely ungated; rate limits bound throughput, not spend (150 msgs/day/IP ≈ $7–37 from one IP). New pure `chatBudgetBlocked` (`loop.ts`) gates chat on the **global** cap only — the per-role `daily_token_budget` deliberately does NOT silence a facet mid-conversation, since "won't speak to you for the rest of the day" is worse than a few cents of overshoot. (2) **Core memory has room now.** `MAX_FILE_CHARS` 8,000 → **16,000** (`engine/memory.ts`); total stays 40,000 (never the binding constraint, and core memory rides every prompt). All five agents had been pinned at 88–98% of the old per-file cap while using ~19% of the total. The load-bearing fix was the **error message**: the per-file cap now says *"split this into another file (e.g. `/memories/people/<name>.md`)"* and only the total cap says *"prune something stale"* — the old undifferentiated wording is exactly what made Career tell a visitor "memory file's capped out, need to trim." `protocol.ts` suggests (never mandates) a per-person file; the reflection prompt now names people-facts as durable-worthy. (3) **`agent_threads` is pruned on both load and persist** (`turn.ts pruneCompactedHistory`, keeps the last 2 compaction checkpoints; migration-free). **Measured, not assumed:** Builder's full 2,183-message thread and a 17-message suffix starting at the newest compaction block both report `input_tokens = 15,021` — verified identical for all five agents. **The API already discards everything before the newest compaction block**, so ~98% of the 2.3–2.9 MB we stored *and uploaded every turn* was inert. Threads with <2 checkpoints are untouched, so young threads never change. **Note for cost chasing: thread size is NOT the per-tick cost driver — cost tracks ROUNDS and output tokens** (researcher's 6-round tick $0.38 vs hobby's 2-round $0.03 on comparable contexts). Expect a one-time cache miss per agent after a deploy that changes the prune point. (4) **Deleted for measured zero usage** (migration `0015`): the whole `agent_presets` subsystem (0 rows; `save_preset`/`list_my_presets` never called once — and `souls/base.md` had to be edited too, or agents would keep reaching for a tool that no longer exists), `decideBoost`'s unreachable `locationBudgetExceeded` branch, and the write-only `chat_sessions.participant_agent_ids` / `pending_operator_note`. **`conversations` + `conversation_turns` are deliberately NOT dropped** — 9 and 73 rows of genuine early town history, kept as an inert archive; their `conversation.*` arms in `observation.ts renderEvents` must also stay so historical `world_events` rows keep parsing.

### Operational gotchas (learned the hard way)
- The **world server is NOT GitHub-connected**: pushing to `main` redeploys Vercel only. Redeploy the world with `railway link` (thomas-town) + `railway up --service world`. `POST /admin/tick/:agent` requires the `x-admin-token` header (token in Railway service vars).
- **`railway.json` uses `/health` as the deploy `healthcheckPath`, so that route's STATUS CODE gates every deploy.** `/health` must therefore ALWAYS return 200 while the process is serving — agent health lives in the response *body* (`ok`, `detail`, per-agent rows). Returning 503 there for an unhealthy agent would mean a circuit-broken agent blocks the very deploy that fixes it. **`/health/agents` is the alertable endpoint** (same body, 503 when the town is genuinely broken); point any uptime monitor at that one. Dormant hours and an exhausted budget deliberately stay `ok` — quiet ≠ broken.
- `@town/contract` exports point at `dist/` — run `pnpm --filter @town/contract build` after pulling before running world/web dev (tsx hid this in dev; plain node in prod does not).
- pnpm build-script approvals live in `pnpm-workspace.yaml` under `allowBuilds` (Railway ignores `onlyBuiltDependencies` alone; missing approvals fail the deploy install).
- `seed.ts` runs on every boot — it must never reset agent `locationId`/state on conflict (only insert-time defaults).
- Tick cadences in `roles/*.yaml` are **45 min** (slowed for the M3 soak to save money while not-yet-real). 45 is the cost-optimal floor: the continuous-thread prefix caches with a **1h TTL that refreshes on read**, so any cadence **< ~60 min keeps every tick a cache hit** (cheap input) — but cross 60 min and the cache goes cold and each tick re-reads the whole thread uncached (~10×), so slowing *past* an hour barely saves. To restore liveliness, lower toward ~12 min; never set above ~55 min. Chat responsiveness is unaffected by cadence (visitor messages are interrupt-driven). Budget: global `DAILY_BUDGET_USD` env (hard, **live value is 25** — per-role caps sum to $21.75, so the boot-time "budget MISMATCH" warning fires only against the local default of 15) + per-role `daily_token_budget` (soft).
- **The town only LIVES 8 hours/day.** `runtime/clock.ts isActiveHours` gates autonomous ticks to 14:00–22:00 Pacific (`TOWN_ACTIVE_START_HOUR` / `TOWN_ACTIVE_HOURS`; `>= 24` disables the window). At a 45-min cadence that's ~10 ticks/agent/day, and **two-thirds of every day is silent** — so the "agents live their lives 24/7" claim is currently false. A long log gap in the middle of the Pacific morning is EXPECTED behaviour, not an outage; check `isActiveHours` before diagnosing silence. Since any cadence <60 min stays cache-warm, 24h at ~55 min costs roughly what 8h at 45 min does — see the overhaul proposal.
- **Actual models are `claude-sonnet-5` for BOTH tick and chat in all five `roles/*.yaml`** — the "Haiku idle / Opus chat" and "Sonnet 4.6" splits described elsewhere in this file and in the M3 vault doc were never implemented. Also: `thinking: {type:"adaptive"}` IS enabled in `turn.ts`, contradicting the M3 doc's note that extended thinking was deliberately left off — that drift is what made threads poisonable. Trust `roles/*.yaml` and `turn.ts` over prose.
- **`drizzle-kit generate` needs a meta snapshot per migration.** If `drizzle/meta/NNNN_snapshot.json` is missing for the newest migration, generate diffs against an older snapshot and emits a DUPLICATE migration of changes already shipped. When hand-writing a migration, also promote the generated snapshot to that migration's number and drop the duplicate SQL, then confirm `pnpm db:generate` prints "No schema changes". Verify the whole chain on a fresh DB: `docker compose up -d postgres` + `pnpm --filter world migrate`.
- Secrets: `scratch.env` (repo root) and `apps/world/.env` are gitignored — never commit or print them.
- **CORS is an allowlist (not wildcard) since M2-G**: `CORS_ORIGINS` (comma-separated, exact-origin match) must be set on the `world` Railway service to the deployed Vercel origin(s), or the live frontend's cross-origin reads are blocked while localhost still works — the classic "works locally, dead in prod" trap. Unset → localhost dev default only. Rate limits + session caps are in-memory (`src/http/rate-limit.ts`, reset on restart — fine for a portfolio).

## Stack decisions (full rationale in the plan §2)

TypeScript pnpm monorepo (`apps/web` = this frontend, `apps/world` = new server, `packages/contract` = shared zod schemas) · Railway monolith + Postgres/pgvector, frontend stays on Vercel · SSE for realtime · **Anthropic SDK's built-in tool runner** (`client.beta.messages.toolRunner()` + `betaZodTool`) for the agent loop — we own the world/scheduler/prompt, never hand-roll the dispatch loop · *(planned tiering was Haiku idle ticks / Opus visitor chat — **as shipped it is `claude-sonnet-5` everywhere**; see the gotchas above)* · memory = per-agent core files (`betaMemoryTool`) + **Hindsight** (self-hosted, verbatim mode) for episodic · Obsidian vault synced via obsidian-git → private repo · Resend (in + out) · Langfuse (v4 OTel) · OpenAI embeddings only.

**Anthropic/Claude work**: the `claude-api` skill is the authority on model IDs, the tool runner, caching, streaming, structured outputs — consult it, don't answer from memory. Current models: Opus 4.8 (`claude-opus-4-8`), Haiku 4.5 (`claude-haiku-4-5`), Sonnet 4.6 (`claude-sonnet-4-6`).

## Git conventions (from Thomas's global rules)

- **Never commit on `main`.** Before any commit: `git fetch origin && git checkout -b thomas/<feature> origin/main`. Every branch must start with `thomas/`.
- PRs are always drafts, titled `[DRAFT] …` (`gh pr create --draft`).
- Durable narrative docs (designs, investigations, plans) go in the **vault**, not `docs/` in the repo. Heavy/throwaway artifacts go in a gitignored `scratch/`.
- Update this CLAUDE.md when you learn something architectural that future sessions need; keep feature-specific detail in the vault plan.
