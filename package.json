{
  "name": "thomas-world",
  "version": "0.0.0",
  "private": true,
  "description": "Thomas's Town monorepo — apps/web (frontend), apps/world (world server), packages/contract (shared schemas).",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev"
  }
}
